/*treat all JS code as newer version, not required to 
write but still make sense to write to ensure we are 
using the newer version of JS.*/
"use strict"; 

// alert(3+3); alert is for the brower, here we are using node.js

console.log(3+3);


let name = "Ujjwal"
let age = 30
let isLoggedIn = false


// Number := 2^53
// bigint 
// string := can be used using single quote or double quote
// boolean
// null := standalone value(type)
        // let x = null
// undefined := variable declared but not defined
// symbol := usually required using react(unique)

// object := 

console.log(typeof "Mike") // string
console.log(typeof null); // object
console.log(typeof undefined); // undefined

